sync:
  defaults:
    mode: "two-way-resolved"
    ignore:
      vcs: true
      paths:
        - "node_modules"
        - ".git"
        - "tmp"
        - ".einar"
        - ".air.log"
        - "bin"

  testboi1:
    alpha: "."
    beta: "exedev@testboi1.exe.xyz:/home/exedev/workspace/testboi1"
