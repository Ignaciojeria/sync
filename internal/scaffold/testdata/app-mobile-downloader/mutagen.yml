sync:
  defaults:
    mode: "two-way-resolved"
    ignore:
      vcs: true
      paths:
        - "node_modules"
        - ".git"
        - "tmp"
        - ".einar"
        - ".air.log"
        - "bin"

  gitinittest5:
    alpha: "."
    beta: "exedev@gitinittest5.exe.xyz:/home/exedev/workspace/gitinittest5"
