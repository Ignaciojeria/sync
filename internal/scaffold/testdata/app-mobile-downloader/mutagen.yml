sync:
  defaults:
    mode: "two-way-resolved"
    ignore:
      vcs: true
      paths:
        - "node_modules"
        - ".git"
        - "tmp"
        - ".einar"
        - ".air.log"
        - "bin"

  fixtests1:
    alpha: "."
    beta: "exedev@fixtests1.exe.xyz:/home/exedev/workspace/fixtests1"
